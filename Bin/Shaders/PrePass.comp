#version 450

layout(std430, set = 0, binding = 0) buffer OutCountBuffer
{
    uint myDrawCount;
} outCountBuffer;

layout(std430, set = 0, binding = 1) buffer OutCountBufferNoDepth
{
    uint myDrawCount;
} outCountBufferNoDepth;

void main() {
    outCountBuffer.myDrawCount = 0;
    outCountBufferNoDepth.myDrawCount = 0;
}